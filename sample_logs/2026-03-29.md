# March 29, 2026 - Work Summary

## 1. GT Conversion Approach Documentation
- Wrote `sample_gt_conversion_approach.md` describing the raw NRRD conversion strategy
- Documented the need to reconstruct semantic masks from NRRD header metadata instead of array index alone
- Captured handling for shared-layer, reordered, typo-labeled, and merged-labelmap cases

## 2. GT Conversion Refactor
- Reworked `convert_gt_cropped.py` to use `Segment{i}_Name`, `Segment{i}_Layer`, and `Segment{i}_LabelValue`
- Added typo-tolerant canonical mapping for `lumen`, `vessel_ob`, and `plaque`
- Added fallback inference for one unknown segment when it is the only missing semantic label
- Added support for both 4D shared/separate-layer `.seg.nrrd` files and 3D merged labelmaps
- Kept final GT labels as:
  - `1` = lumen
  - `2` = vessel wall (`vessel_ob` minus lumen)
  - `0` = background
- Used plaque for QC/reconstruction context only, not as a final output class

## 3. GT Conversion Logging
- Added tee logging so `convert_gt_cropped.py` writes to both console and `sample_logs/`
- Updated log filename format to `convert_gt_cropped_log_ddmmyyyy_HHMM.txt`
- Renamed the generated conversion log to match the final naming convention

## 4. Conversion Run Review
- Reviewed `sample_logs/convert_gt_cropped_log_29032026_0708.txt`
- Confirmed 100 successful GT conversions and 1 skipped case
- The skipped case was `053_VL2` due to missing crop parameters, not a reconstruction failure

## 5. Evaluation Script Review
- Reviewed `sample_evaluate_predictions.py` and `sample_evaluate_lumen.py`
- Confirmed both scripts write CSV output
- Confirmed `sample_evaluate_lumen.py` duplicates a subset of `sample_evaluate_predictions.py`

## 6. New Unified Evaluator
- Added a new self-contained `sample_evaluate.py`
- Default behavior evaluates both lumen and vessel wall
- Added mutually exclusive flags:
  - `--lumen-only`
  - `--vessel-only`
- Added wide-format CSV output optimized for downstream statistical analysis
- Included one row per case with separate metric columns for lumen and vessel wall

## 7. Communication Draft
- Drafted an email update summarizing completion of evaluation, planned statistics, and next steps
- Included request to meet in the cath lab tomorrow for detailed review
